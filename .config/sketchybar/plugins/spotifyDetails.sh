#!/bin/bash
sketchybar -m --set $NAME popup.drawing=toggle
# sketchybar -m --set spotifyControls popup.drawing=toggle
# sketchybar -m --set spotifyPlayPauseClone drawing=toggle
# sketchybar -m --set spotifyNextClone drawing=toggle

